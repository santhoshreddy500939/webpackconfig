changing process from normal files into react:
        -Create individual componenents 
        -import into index.js & crate a component for holding those component/just in const variable
        -react.render(<App/>,document.getElementById('app')
        -add <div id="app"></div> into index.html


        installing the npm packages webpack webpack-cli --save-dev
                                    react react-dom --save
                                    @babel-core @babel-preset-env @babel-preset-react babel-loader
                                    webpack-dev-server --save-dev 
                                    webpack-merge --save-dev
